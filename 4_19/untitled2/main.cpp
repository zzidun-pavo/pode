#include <bits/stdc++.h>

using namespace std;

static int n, e;
static int m[1200];

int main()
{
    scanf("%d%d", &n, &e);
    for(int i = 1; i <= n; ++i)
    {
        scanf("%d", &m[i]);
    }
}
